<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vybe">

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MyRequestsActivity"
            android:screenOrientation="nosensor"></activity>

        <activity android:name=".SearchProfilesActivity"
            android:screenOrientation="nosensor"/>

        <activity android:name=".SocialActivity"
            android:screenOrientation="nosensor"/>

        <activity android:name=".ViewProfileActivity"
            android:screenOrientation="nosensor"/>

        <activity android:name=".CreateAccountActivity"
            android:screenOrientation="nosensor"/>

        <activity android:name=".ViewVibeActivity"
            android:screenOrientation="nosensor"/>

        <activity android:name=".AddEdit.AddEditVibeEventActivity"
            android:screenOrientation="nosensor"/>

        <activity android:name=".MyVibesActivity"
            android:screenOrientation="nosensor"/>

        <activity android:name=".MapViewActivity"
            android:screenOrientation="nosensor"/>

        <activity android:name=".MainActivity"
            android:screenOrientation="nosensor">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_map_api_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>